# TODO

- [x] Add “AI Cow Health (LSTM vs Static)” section to `app/employees/page.tsx` (bottom).
  - [x] Include two model cards: LSTM (model.pkl) and Static model (static model pkl).
  - [x] Hardcode two demo instances (walking vs standing) and show “cow is healthy” for both.
  - [x] Ensure labels/wording match requirement: walking for one instance, standing for the other.
- [ ] Verify TypeScript/ESLint build (if available in environment).


